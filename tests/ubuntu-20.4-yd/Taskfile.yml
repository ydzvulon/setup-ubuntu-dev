# https://taskfile.dev

version: '3'

vars:
  GREETING: Hello, World!
  DIMAGE: ubuntu-20.4-yd

tasks:
  default:
    cmds:
      - task -l
    silent: true
  
  build:
    desc: _
    cmds:
      - docker build -t {{.DIMAGE}} .

